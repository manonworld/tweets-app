# EGFWD Tweets Project

A simple tweets application with fake data.

You can create a new tweet, respond to tweets, like tweets and view tweets.


### Prerequisites:

Docker
Docker Compose


### Installation:

``` make install ```


### Running:

``` make run ```

### Cleaning:

``` make clean ```